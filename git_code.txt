git status  #確認

git branch 　#ブランチ確認
git branch ""　 #ブランチ作成
git branch -d ""　#ブランチ消去

<ブランチ変更>
git checkout ブランチ名(issue/#5,main)

<自分のブランチに入れる>
git add .　#チェックポイント
git commit -m "コメント"
git push origin 自分のブランチ名(issue/#5)
↓完成したら...
gitでpull request

<メインのものを引っ張ってくる>
git pull origin main #自分のブランチ内で行う


